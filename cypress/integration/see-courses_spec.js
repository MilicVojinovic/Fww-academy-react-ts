/// <reference types="cypress" />


describe('see-courses', () => {
	it('teacher see own courses', () => {

		// cy.get('[placeholder=E-mail]').type('jason.statham@gmail.com');

		cy.Login('jason.statham@gmail.com', 'mightyme')

		// cy.findByText(/prijavi se/i).click();

		// cy.get('[data-testid="side-menu-button:Lista Vaših kurseva"]').click();

		// cy.get('[data-testid="side-menu-button:Lista Vaših kurseva"]').click();

		// cy.get('#select-course').click();
		
		// cy.contains(/vue 2/i).click();

		// cy.get('#select-teacher').click();

		// cy.contains(/Milos Medan/i).click();

		// cy.contains(/pošalji/i).click();

		// cy.wait(4000)

		

	})
})